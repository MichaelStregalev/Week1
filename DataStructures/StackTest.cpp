/*#include "Stack.h"

int main()
{
	Stack s = {};

	initStack(&s);

	std::cout << "Pushing elements into the stack:" << std::endl;

	push(&s, 8);
	push(&s, 9);
	push(&s, 10);
	push(&s, 11);

	printNodeList(&(s.head));

	std::cout << std::endl;

	std::cout << "Is the stack empty? 1 - true, 0 - false" << std::endl;

	std::cout << isEmpty(&s) << std::endl;

	std::cout << "Is the stack full? 1 - true, 0 - false" << std::endl;

	std::cout << isFull(&s) << std::endl;

	std::cout << "Popping the first element:" << std::endl;

	std::cout << "POP: " << pop(&s) << std::endl;

	printNodeList(&(s.head));

	std::cout << std::endl;

	std::cout << "Cleaning the stack:" << std::endl;

	cleanStack(&s);

	printNodeList(&(s.head));

	std::cout << "Popping an empty stack:" << std::endl;

	std::cout << pop(&s) << std::endl;

	std::cout << "Is the stack empty? 1 - true, 0 - false" << std::endl;

	std::cout << isEmpty(&s) << std::endl;

	std::cout << "Is the stack full? 1 - true, 0 - false" << std::endl;

	std::cout << isFull(&s) << std::endl;
}*/